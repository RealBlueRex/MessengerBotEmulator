package org.hui1601.emulator.platform.application.actions;

public class DeleteAction {
	/* public static void update(ListView listView, List<?> list)
	{
        listView.getItems().removeAll(listView.getSelectionModel().getSelectedItems());
	} */
}
